<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Company>SuRGeoNix</Company>
    <Authors>SuRGeoNix</Authors>
    <PackageLicenseExpression>LGPL-3.0-or-later</PackageLicenseExpression>
    <Description>Bittorrent library for clients &amp; streaming purposes</Description>
    <PackageLicenseFile></PackageLicenseFile>
    <PackageProjectUrl>https://github.com/SuRGeoNix/BitSwarm</PackageProjectUrl>
    <PackageIcon>BitSwarm.png</PackageIcon>
    <RepositoryUrl>https://github.com/SuRGeoNix/BitSwarm</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>bitswarm bittorrent torrent client streaming dht</PackageTags>
    <Copyright>© SuRGeoNix 2020</Copyright>
    <Version>2.3.3</Version>
    <PackageReleaseNotes>- Adding support for torrents that the size of Last Block of each Piece is not divided by 16384
- Fixing an issue with SHA-1 failures and ban (blocks diff)
- Fixing an issue with Peers that disconnect wouldn't reset the requested pieces
- Fixing a crashing issue by opening a session file (.bsf)
- Minor changes on Stream files
- Updating APF library</PackageReleaseNotes>
    <FileVersion>2.3.3.0</FileVersion>
    <AssemblyVersion>2.3.3.0</AssemblyVersion>
    <RootNamespace>SuRGeoNix.BitSwarmLib</RootNamespace>
    <Product>BitSwarm</Product>
    <AssemblyName>BitSwarmLib</AssemblyName>
    <PackageId>BitSwarm</PackageId>
	<GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="APF" Version="1.2.0" />
    <PackageReference Include="BencodeNET" Version="3.1.4" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\Images\BitSwarm.png">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>